#include <iostream>
using namespace std;

double Sum(int n){
    double sum = 0;
    for(int i = 1; i <= n; i++){
        sum += 1.0/(i*i + 2*i + 1);
    }
    return sum;
}
int main(){
    int n;
    cout << "n: ";
    cin >> n;
    cout << Sum(n);
    return 0;
}
