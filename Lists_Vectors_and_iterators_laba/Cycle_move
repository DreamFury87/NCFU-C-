#include <iostream>
using namespace std;

int main(){
    int nums[100], n, tmp;
    cin >> n;
    for(int i = 0; i<n; i++)
        cin >> nums[i];

    for(int i = n - 1; i > 0; i--){
        swap(nums[i], nums[i-1]);
    }

    for(int i = 0; i<n; i++)
        cout << nums[i] << " ";

    return 0;
}
